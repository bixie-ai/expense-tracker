import { type ReactNode } from 'react';

export default function Dashboard(): ReactNode {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Migrated React route — Dashboard</p>
    </div>
  );
}
